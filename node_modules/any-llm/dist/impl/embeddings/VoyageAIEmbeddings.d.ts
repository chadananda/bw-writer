import { EmbeddingBase } from '../../models/Base';
import { EmbeddingModel, EmbeddingResult } from '../../models/types';
export declare class VoyageAIEmbedding implements EmbeddingBase {
    apiKey: string;
    constructor(apiKey: string);
    generateEmbeddings(model: EmbeddingModel, texts: string[], truncation?: boolean, dimensions?: number, inputType?: 'query' | 'document'): Promise<EmbeddingResult>;
}
