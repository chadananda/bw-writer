import { TokenizerBase } from '../../models/Base';
export declare class AnthropicTokenizer implements TokenizerBase {
    private tokenizer;
    constructor();
    encode(text: string): number[];
    decode(tokens: number[]): string;
    countTokens(text: string): number;
}
