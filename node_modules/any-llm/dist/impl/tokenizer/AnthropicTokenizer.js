"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.AnthropicTokenizer = void 0;
const tiktoken_1 = require("tiktoken");
const claude_json_1 = __importDefault(require("./data/claude.json"));
class AnthropicTokenizer {
    constructor() {
        this.tokenizer = new tiktoken_1.Tiktoken(claude_json_1.default.bpe_ranks, claude_json_1.default.special_tokens, claude_json_1.default.pat_str);
    }
    // No changes required
    encode(text) {
        const encoded = this.tokenizer.encode(text.normalize('NFKC'), 'all');
        return Array.from(encoded);
    }
    decode(tokens) {
        const decoded = this.tokenizer.decode(new Uint32Array(tokens));
        return new TextDecoder().decode(decoded);
    }
    countTokens(text) {
        return this.encode(text).length;
    }
}
exports.AnthropicTokenizer = AnthropicTokenizer;
