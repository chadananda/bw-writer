import { Stream } from 'cohere-ai/core';
import { ChatBase } from '../../models/Base';
import { ChatMessage, LLMSettings } from '../../models/types';
import { Cohere } from 'cohere-ai';
import { StreamingTextResponse } from 'ai';
export declare class CohereChat extends ChatBase {
    private cohere;
    fallBackSystemMessage: string;
    constructor(apiKey: string);
    generateChatCompletion(chatSettings: LLMSettings, messages: ChatMessage[]): Promise<Stream<Cohere.StreamedChatResponse>>;
    static messageConversion(message: ChatMessage): Cohere.ChatMessage;
    generateChatCompletionStream(chatSettings: LLMSettings, messages: ChatMessage[]): Promise<StreamingTextResponse>;
}
