import Anthropic from '@anthropic-ai/sdk';
import { MessageParam } from '@anthropic-ai/sdk/resources';
import { Stream } from '@anthropic-ai/sdk/streaming';
import { ChatBase } from '../../models/Base';
import { ChatMessage, LLMSettings } from '../../models/types';
import { StreamingTextResponse } from 'ai';
export declare class AnthropicChat extends ChatBase {
    private anthropic;
    fallBackSystemMessage: string;
    constructor(apiKey: string);
    generateChatCompletion(chatSettings: LLMSettings, messages: ChatMessage[]): Promise<Stream<Anthropic.Messages.MessageStreamEvent>>;
    messageConversion(message: ChatMessage): MessageParam;
    generateChatCompletionStream(chatSettings: LLMSettings, messages: ChatMessage[]): Promise<StreamingTextResponse>;
}
