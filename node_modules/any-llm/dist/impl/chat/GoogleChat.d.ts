import { Content, Role } from '@google/generative-ai';
import { ChatBase } from '../../models/Base';
import { ChatMessage, ChatRoles, LLMSettings } from '../../models/types';
import { StreamingTextResponse } from 'ai';
export declare class GoogleChat extends ChatBase {
    private googleAI;
    constructor(googleGeminiApiKey: string);
    static toGoogleRole(role: ChatRoles): Role;
    static convertMessages(message: ChatMessage): Content;
    generateChatCompletion(chatSettings: LLMSettings, messages: ChatMessage[]): Promise<ReadableStream>;
    generateChatCompletionStream(chatSettings: LLMSettings, messages: ChatMessage[]): Promise<StreamingTextResponse>;
}
