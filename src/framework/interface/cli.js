#!/usr/bin/env node
/**
 * Command Line Interface for MCP Server (Wrapper)
 * This is a simple wrapper that imports and runs the CLI core from the utils directory
 * with the application definition from index.js
 */
import '../utils/cli-core.js';
